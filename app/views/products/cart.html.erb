<div>
	<h2>Cart</h2>

	<% if session[:cart].empty? %>
		<p>Your cart is empty</p>
	<%else%>
		<%= render @cart_contents %>
	<%end%>	
</div>

<div>
	<%= link_to "Clear Cart", remove_cart_path %>
</div>

<div>
	<%= render :partial => "form" %>
</div>

